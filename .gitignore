__pycache__/
*.py[cod]
*$py.class
.DS_Store
.venv/
venv/
env/
# Avoid pushing the local datasets if they are considered external or too large
# But in this case, the user seems to have them in the repo
